apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: logan-api
  namespace: logan
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: logan-api
  minReplicas: 2
  maxReplicas: 8
  metrics:
    - type: Resource
      resource:
        name: cpu
        target:
          type: Utilization
          averageUtilization: 70
---
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: logan-worker
  namespace: logan
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: logan-worker
  minReplicas: 2
  maxReplicas: 10
  metrics:
    - type: Resource
      resource:
        name: cpu
        target:
          type: Utilization
          averageUtilization: 75

apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: postgres-data
  namespace: logan
spec:
  accessModes: ["ReadWriteOnce"]
  resources:
    requests:
      storage: 20Gi
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: postgres
  namespace: logan
spec:
  replicas: 1
  selector:
    matchLabels:
      app: postgres
  template:
    metadata:
      labels:
        app: postgres
    spec:
      containers:
        - name: postgres
          image: postgres:16
          env:
            - name: POSTGRES_DB
              value: logan
            - name: POSTGRES_USER
              value: logan
            - name: POSTGRES_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: logan-secrets
                  key: LOGAN_SECRET_KEY
          ports:
            - containerPort: 5432
          volumeMounts:
            - name: data
              mountPath: /var/lib/postgresql/data
      volumes:
        - name: data
          persistentVolumeClaim:
            claimName: postgres-data
---
apiVersion: v1
kind: Service
metadata:
  name: postgres
  namespace: logan
spec:
  selector:
    app: postgres
  ports:
    - port: 5432
      targetPort: 5432

{
  "name": "logan-platform",
  "private": true,
  "workspaces": [
    "apps/web"
  ],
  "scripts": {
    "build": "npm run build --workspace @logan/web",
    "lint": "npm run lint --workspace @logan/web",
    "typecheck": "npm run typecheck --workspace @logan/web"
  }
}
